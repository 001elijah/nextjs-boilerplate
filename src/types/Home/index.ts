export interface HomeHeroProps {
  hero: {
    heading: string
    hook: string
    howItWorks: string
    intro: string
    tryForFree: string
  }
}

export interface PricingProps {
  pricing: {
    action: string
    heading: string
    hook: string
    intro: string
    modalAction: string
    modalHeading: string
    modalPrompt: string
  }
}
