import { ContentPresetAddForm } from '@/components'
import { IContentsProps } from '@/types'

export const CreateContentPreset = ({ contents }: IContentsProps) => <ContentPresetAddForm contents={contents} />
